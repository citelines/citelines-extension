{
  "name": "youtube-annotator-extension",
  "private": true,
  "scripts": {
    "build": "esbuild src/content/main.js --bundle --outfile=content.bundle.js --format=iife",
    "watch": "esbuild src/content/main.js --bundle --outfile=content.bundle.js --format=iife --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
